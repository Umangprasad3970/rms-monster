# Copy this file to .env (and load it with your process manager, or export
# the variables directly) before running the server.

# Required to use /admin.html — pick a long random string.
ADMIN_KEY=change-this-to-a-long-random-string

# Optional — defaults to 3000.
PORT=3000
